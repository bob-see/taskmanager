datasource db {
  provider = "mysql"
}
generator client {
  provider = "prisma-client-js"
  engineType = "binary"
}

model User {
  id           String    @id @default(cuid()) @db.VarChar(36)
  name         String
  email        String    @unique @db.VarChar(191)
  passwordHash String
  role         String    @default("user") @db.VarChar(32)
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt

  profiles     Profile[]

  @@index([email])
  @@map("user")
}

model Task {
  id               String   @id @default(cuid()) @db.VarChar(36)
  title            String
  notes            String?  @db.Text
  dueAt            DateTime?
  completedAt      DateTime?
  completedOn      DateTime?
  orderIndex       Int?
  isPriority       Boolean  @default(false)
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt
  startDate        DateTime @default(now())
  category         String?
  profileId        String   @default("default") @db.VarChar(36)
  projectId        String?  @db.VarChar(36)
  recurrenceSeriesId String? @db.VarChar(36)
  repeatEnabled    Boolean  @default(false)
  repeatPattern    String?
  repeatDays       Int?
  repeatWeeklyDay  Int?
  repeatMonthlyDay Int?

  profile Profile @relation(fields: [profileId], references: [id], onDelete: Cascade)
  project Project? @relation(fields: [projectId], references: [id], onDelete: SetNull)

  @@index([profileId, startDate])
  @@index([profileId, dueAt])
  @@index([profileId, completedOn])
  @@index([profileId, completedOn, orderIndex])
  @@index([profileId, recurrenceSeriesId])
  @@index([projectId])
  @@unique([profileId, recurrenceSeriesId, startDate])
  @@map("task")
}

model Profile {
  id            String    @id @default(cuid()) @db.VarChar(36)
  userId        String?   @db.VarChar(36)
  name          String
  order         Int
  defaultView   String?
  averageBasis  String?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  user          User?     @relation(fields: [userId], references: [id], onDelete: Cascade)
  tasks         Task[]
  projects      Project[]
  timeEntries   TimeEntry[]
  @@index([userId])
  @@map("profile")
}

model Project {
  id        String    @id @default(cuid()) @db.VarChar(36)
  name      String
  startDate DateTime  @default(now())
  dueAt     DateTime?
  category  String?
  orderIndex Int?
  archived  Boolean   @default(false)
  collapsed Boolean   @default(false)
  isPriority Boolean  @default(false)
  createdAt DateTime  @default(now())
  updatedAt DateTime  @updatedAt
  profileId String    @db.VarChar(36)

  profile Profile @relation(fields: [profileId], references: [id], onDelete: Cascade)
  tasks   Task[]

  @@index([profileId, createdAt])
  @@index([profileId, orderIndex])
  @@map("project")
}

model TimeEntry {
  id              String   @id @default(cuid()) @db.VarChar(36)
  profileId       String   @db.VarChar(36)
  entryDate       DateTime
  startTime       DateTime
  endTime         DateTime?
  durationMinutes Int?
  loggedMinutes   Int?
  roundingMode    String?
  notes           String?  @db.Text
  source          String
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  profile Profile @relation(fields: [profileId], references: [id], onDelete: Cascade)

  @@index([profileId, entryDate])
  @@index([entryDate])
  @@index([endTime])
  @@map("timeentry")
}

model ActivityLog {
  id          String   @id @default(cuid()) @db.VarChar(36)
  userId      String   @db.VarChar(36)
  profileId   String?  @db.VarChar(36)
  taskId      String?  @db.VarChar(36)
  projectId   String?  @db.VarChar(36)
  timeEntryId String?  @db.VarChar(36)
  type        String   @db.VarChar(64)
  description String   @db.Text
  createdAt   DateTime @default(now())

  @@index([userId, createdAt])
  @@index([type])
  @@index([profileId])
  @@index([taskId])
  @@index([projectId])
  @@index([timeEntryId])
  @@map("activitylog")
}
